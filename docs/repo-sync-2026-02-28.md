# Repository Sync — 2026-02-28

## Summary

Synced the local `main` branch with `origin/main` (GitHub: `juren53/system-monitor`).
The local branch was 16 commits behind and was updated via a clean fast-forward merge.

---

## Starting State

| Item | Value |
|---|---|
| Local branch | `main` |
| Remote | `origin` → `https://github.com/juren53/system-monitor` |
| Local HEAD | `6ba11f3` |
| Remote HEAD | `22c4389` |
| Commits behind | 16 |
| Merge type | Fast-forward (no divergence) |

---

## Steps Performed

### 1. Inspect remotes and local status

```bash
git remote -v
git status
git log --oneline -10
```

Confirmed a single remote (`origin`) and that the branch could be fast-forwarded.

### 2. Fetch and preview incoming commits

```bash
git fetch origin
git log --oneline HEAD..origin/main
```

Output — 16 commits not yet local:

```
22c4389 Fix icons/app.ico: convert from symlink to regular file
0069072 moved .claude/settings.local.json to .gitignore
97dcd77 Bump version to v0.5.5, update CHANGELOG
f71c43d Fix dark mode not restored on startup
f076423 Fix washed-out menu bar text in dark mode
53e225d Bump version to v0.5.4, update CHANGELOG
cdf227b Fix hover labels to respect current theme (dark/light)
82d0dbe Update CHANGELOG: icon fix and install script improvements for v0.5.3
5bca6c6 Use exe-embedded icon in Start Menu shortcut, flush icon cache
ccc16ba Regenerate app.ico from multi-resolution PNGs
6feced8 Add v0.5.3 entry to CHANGELOG
8fbc83c Bump version to v0.5.3
a80b2ac Add Windows desktop integration instructions to README
b9f81db Add install-windows.ps1: Start Menu shortcut and taskbar pin
c672c79 Resolve merge conflict in settings.local.json: merge all permissions
9d74622 Resolve merge conflict in settings.local.json: merge stash permissions
```

### 3. Pull (fast-forward)

```bash
git pull origin main
```

Git fast-forwarded `main` from `6ba11f3` to `22c4389`.

---

## Files Changed (11 files, 203 insertions / 18 deletions)

| File | Change |
|---|---|
| `.claude/settings.local.json` | Added permissions entries |
| `.gitignore` | Added `settings.local.json` exclusion |
| `CLAUDE.md` | Version reference updated to v0.5.5 |
| `README.md` | Added Windows desktop integration instructions |
| `docs/CHANGELOG.md` | Appended v0.5.3, v0.5.4, v0.5.5 entries (59 lines) |
| `icons/app.ico` | Converted from symlink → regular binary file (multi-resolution) |
| `install-windows.ps1` | New script: creates Start Menu shortcut and pins to taskbar |
| `src/sysmon.py` | Minor additions (5 lines) |
| `src/sysmon/constants.py` | Version constants bumped to v0.5.5 |
| `src/sysmon/theme.py` | Dark mode restoration fix (40 lines added) |
| `src/sysmon/window.py` | Menu bar dark mode text + hover label theme fixes |

---

## Versions Introduced

| Version | Key changes |
|---|---|
| v0.5.3 | Windows installer script (`install-windows.ps1`), app.ico rebuilt from PNGs |
| v0.5.4 | Dark mode hover label fix, washed-out menu bar text fix |
| v0.5.5 | Dark mode not restored on startup fix, `app.ico` symlink → real file |

---

## End State

```
Local HEAD : 22c4389  (matches origin/main)
Branch     : main (up to date)
Untracked  : PLAN_SysMon-addition-of-Memory-graph.md  (pre-existing, untouched)
```
